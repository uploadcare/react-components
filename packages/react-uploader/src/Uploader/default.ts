import { PACKAGE_VERSION } from "@uploadcare/blocks";

export const CTX_NAME = "my-uploader";

export const CSS_SRC_REGULAR = `https://cdn.jsdelivr.net/npm/@uploadcare/blocks@${PACKAGE_VERSION}/web/lr-file-uploader-regular.min.css`;

export const CSS_SRC_MINIMAL = `https://cdn.jsdelivr.net/npm/@uploadcare/blocks@${PACKAGE_VERSION}/web/lr-file-uploader-mininal.min.css`;

export const CSS_SRC_INLINE = `https://cdn.jsdelivr.net/npm/@uploadcare/blocks@${PACKAGE_VERSION}/web/lr-file-uploader-inline.min.css`;
